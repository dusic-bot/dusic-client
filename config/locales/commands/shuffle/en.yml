commands:
  shuffle:
    title: Audio Player
    text:
      queue_shuffled: Queue shuffled
    errors:
      unavailable_in_dm: |-
        Audio queue is only available in guilds
      not_authorized: |-
        This command can only be executed by server DJs
